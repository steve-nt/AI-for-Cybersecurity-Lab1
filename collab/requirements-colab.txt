# Colab already ships everything below except tabulate.
# Install with:  pip install -q -r requirements-colab.txt
#
# NOTE: this is deliberately NOT the same as the main project's requirements.txt.
# That file pins tensorflow<2.18 and torch, which forces numpy<2 and a runtime
# restart on Colab. No script in src/ imports tensorflow or torch, so installing
# them here buys nothing and risks breaking the Colab runtime.
scikit-learn>=1.3.0
pandas>=2.0.0
numpy
matplotlib>=3.7.0
joblib>=1.4.0
tabulate>=0.9.0
